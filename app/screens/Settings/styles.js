import GlobalStyles from '../../styles';

const styles = {
    section: {
        marginBottom: 10,
        alignItems: 'center'
    },

    title: {
        fontSize: 15,
        marginBottom: 5,
        textAlign: 'center'
    },

    errorText: {
        color: '#ce2720',
        marginBottom: 5
    }
};

export default Object.assign({}, GlobalStyles, styles);
